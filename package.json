{
  "private": true,
  "name": "bidipeppercrap-worker",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "fix-supabase": "patch-package cross-fetch",
    "postinstall": "patch-package"
  },
  "author": "bidipeppercrap <bidipeppercrap@outlook.com>",
  "license": "MIT",
  "devDependencies": {
    "patch-package": "^6.2.2",
    "postinstall-postinstall": "^2.1.0",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^1.24.0",
    "cross-fetch": "^3.0.6"
  }
}
